- gesamte Aufgabe 2 hierher bringen, ok

- Prioritäten für ISR festlegen

- berechneGeschwindigkeit Aufruf in main korrigieren, ok

- aktuelle Phasen und Phasenwechseln in den ISRs bestimmen

- prüfen, ob die superloop nur atomare Befehle enthält, bz. Synchronisation

- kompilierbar machen, multiple Definitionen beheben, ok

- get_drehgeberdaten2 oder get_drehgeberdaten implementieren

- Zwisschentest (get_drehgeberdaten-2), 