

//wandelt eine int Zahl in einen String um, der ausgegeben werden kann.
void intToString(int n, char *str);

//Oberste Zahl ausgeben und auf Stack liegen lassen
int stack_peek(void);

//Formale paramter mit int-Zahl und Adresse zu Char-Array (12 zeichen)
void intToString(int n, char *str);

//Druckt den gesamten Stack aus
int stack_print_all(void);

//Vertauscht die obersten zwei Zahlen des Stacks
int stack_swap(void);

//Verdoppelt den obersten Wert auf dem Stack
int stack_double(void);